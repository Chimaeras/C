void sort(int B[], int size)
{
	int flag;
	for (int i = 1;i < size;++i)
	{
		flag = B[i];						        //标志数记录当前元素；
		int j = i - 1;
		while (j >= 0 && flag < B[j])		//标志数和前面的元素进行比较；
		{
			B[j + 1] = B[j];
			--j;
		}
		B[j + 1] = flag;					      //插入目标元素；
	}
}
